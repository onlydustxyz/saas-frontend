export enum HackathonStatus {
  Live = "live",
  Open = "open",
  Closed = "closed",
}

export enum HackathonEventStatus {
  Planned = "planned",
  Terminated = "terminated",
  Highlight = "highlight",
}
